# Remote tmux config (C-b prefix)
# Used on devbox/GPU machines via SSH

# Shared settings
source-file ~/.config/tmux/tmux.shared.conf

# Prefix: C-b (default, distinct from local C-a)
set -g prefix C-b
bind C-b send-prefix

# Copy to system clipboard (Linux xclip)
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -i -f -selection primary | xclip -i -selection clipboard"

# Toggle status bar
bind s set-option -g status

# Status bar — hostname + session name (green to indicate remote)
set -g status-position top
set -g status-style "bg=#0B0E14,fg=#475266"
set -g status-left-length 30
set -g status-left "#[fg=#1e1e2e,bg=#a6e3a1,bold] #h:#S #[default] "
set -g status-right "#{?client_prefix,#[fg=#95E6CB,bold] C-b #[default],}#[fg=#475266] %H:%M "

# Window format
set -g window-status-format "#[fg=#475266] #I:#W "
set -g window-status-current-format "#[fg=#BFBDB6,bold] #I:#W "

# Initialize TPM (keep at bottom)
run -b '~/.tmux/plugins/tpm/tpm'
